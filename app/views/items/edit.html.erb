<h1>Edit Item</h1>

<%= simple_form_for @item do |f| %>
  <%= f.input :name, label: "Name" %>
  <%= f.input :description, label: "Description" %>
  <%= f.input :price, label: "Price", as: :decimal %>

  <%= f.submit "Update Item" %>
<% end %>

<%= link_to "Back to Items", items_path %>
